<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE generatorConfiguration
        PUBLIC "-//mybatis.org//DTD MyBatis Generator Configuration 1.0//EN"
        "http://mybatis.org/dtd/mybatis-generator-config_1_0.dtd">

<generatorConfiguration>
    <context id="default" targetRuntime="MyBatis3" defaultModelType="flat">

        <!-- Automatically check keywords and add back quotes to keywords ` , Some database doesn't support-->
        <property name="autoDelimitKeywords" value="true"/>
        <property name="beginningDelimiter" value="`"/>
        <property name="endingDelimiter" value="`"/>

        <plugin type="org.mybatis.generator.plugins.UnmergeableXmlMappersPlugin"/>

        <!-- add toString() -->
        <plugin type="org.mybatis.generator.plugins.ToStringPlugin"/>

        <commentGenerator>
            <property name="suppressAllComments" value="true"/>
        </commentGenerator>

        <jdbcConnection driverClass="com.mysql.cj.jdbc.Driver"
                        connectionURL="jdbc:mysql://192.168.83.138:3306/tiny-twitter-dev"
                        userId="root"
                        password="password">
        </jdbcConnection>

        <!-- pojo class-->
        <javaModelGenerator targetProject="src\main\java"
                            targetPackage="com.ziyuan.pojo"/>

        <!-- mapper xml -->
        <sqlMapGenerator targetProject="src\main\resources"
                         targetPackage="mapper"/>

        <!-- mapper class -->
        <javaClientGenerator targetProject="src\main\java"
                             targetPackage="com.ziyuan.mapper"
                             type="XMLMAPPER"/>

        <!--tables-->
        <table tableName="user"/>
        <table tableName="relation"/>
    </context>
</generatorConfiguration>
